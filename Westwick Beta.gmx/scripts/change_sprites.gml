/// change_sprites(sprite_index, image_index, image_speed);
var sprite = argument0;
var index = argument1;
var spd = argument2;

// Change sprite
if (sprite_index != sprite) {
    sprite_index = sprite;
    image_index = index;
    image_speed = spd;
}
